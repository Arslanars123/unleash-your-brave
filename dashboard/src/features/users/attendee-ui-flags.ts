/** Dashboard attendee UI toggles — data/API unchanged; flip to re-show fields. */
export const ATTENDEE_UI = {
  showPoints: false,
  showStatus: false,
  showProfileCompleted: false,
  showSuspendAction: false,
  showIsVip: false,
} as const;
