# Production API (App Runner — client account, us-east-1)
API_BASE_URL=https://p7afpmrcqg.us-east-1.awsapprunner.com/api/v1

# Local development (uncomment to override):
# API_BASE_URL=http://localhost:4000/api/v1

# Optional: rewrite legacy /uploads/... paths to S3 (new uploads already return full S3 URLs).
MEDIA_BASE_URL=https://unleash-media-381491933714.s3.us-east-1.amazonaws.com

# Same key as dashboard VITE_GOOGLE_MAPS_API_KEY. Enable Maps SDK for Android + iOS.
# GOOGLE_MAPS_API_KEY=
