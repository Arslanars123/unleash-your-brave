API_BASE_URL=http://localhost:4000/api/v1

# Same key as dashboard VITE_GOOGLE_MAPS_API_KEY. Enable Maps SDK for Android + iOS.
# Also wire it into AndroidManifest / AppDelegate (see docs note in README or PUSH docs).
# GOOGLE_MAPS_API_KEY=
